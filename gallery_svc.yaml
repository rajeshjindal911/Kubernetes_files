apiVersion: v1
kind: Service
metadata:
  creationTimestamp: null
  labels:
    run: gallery
  name: gallery
spec:
  ports:
  - name: "80"
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    run: gallery
  type: NodePort
status:
  loadBalancer: {}
